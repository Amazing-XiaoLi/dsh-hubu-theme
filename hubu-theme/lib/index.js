// Hubu (湖北大学) theme — host-side bootstrap entry.
// All visual work happens in the browser client half (lib/client.js);
// this entry only needs to resolve so the loader can mount the package.
export function apply(ctx) {}